            ->addForeignKey(
                $setup->getFkName(
                    '{{ module.getNamespace()|snake }}_{{ module.getNamespace()|snake }}_{{ entity.getNameSingular()|snake }}',
                    '{{ attribute.getCode() }}',
                    'eav_attribute_set',
                    'attribute_set_id'
                ),
                '{{ attribute.getCode() }}',
                $setup->getTable('eav_attribute_set'),
                'attribute_set_id',
                Table::ACTION_CASCADE
            )
