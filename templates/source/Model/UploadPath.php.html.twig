{% if module.hasAttributeType('image') or module.hasAttributeType('file') %}
<?php{{ module.getFormattedLicense('php') }}
namespace {{ module.getData('namespace') }}\{{ module.getData('module_name') }}\Model;

class UploadPath
{
{% for entity in module.getEntities() %}
{% if entity.hasAttributeType('image') %}
    /**
     * {{ entity.getData('label_singular')|capitalize }} tmp image path
     *
     * @var string
     */
    const {{ entity.getData('name_singular')|snake|upper }}_IMAGE_TMP_PATH = '{{ module.getData('namespace')|snake }}_{{ module.getData('module_name')|snake }}/tmp/{{ entity.getData('name_singular')|snake }}/image';

    /**
     * {{ entity.getData('label_singular')|capitalize }} image path
     *
     * @var string
     */
    const {{ entity.getData('name_singular')|snake|upper }}_IMAGE_PATH = '{{ module.getData('namespace')|snake }}_{{ module.getData('module_name')|snake }}/{{ entity.getData('name_singular')|snake }}/image';
{% endif %}
{% if entity.hasAttributeType('file') %}
    /**
     * {{ entity.getData('label_singular')|capitalize }} tmp file path
     *
     * @var string
     */
    const {{ entity.getData('name_singular')|snake|upper }}_FILE_TMP_PATH = '{{ module.getData('namespace')|snake }}_{{ module.getData('module_name')|snake }}/tmp/{{ entity.getData('name_singular')|snake }}/file';

    /**
     * {{ entity.getData('label_singular')|capitalize }} file path
     *
     * @var string
     */
    const {{ entity.getData('name_singular')|snake|upper }}_FILE_PATH = '{{ module.getData('namespace')|snake }}_{{ module.getData('module_name')|snake }}/{{ entity.getData('name_singular')|snake }}/file';
{% endif %}
{% endfor %}
}
{% endif %}
