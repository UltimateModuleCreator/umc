{# deprecated #}
<div class="nav nav-tabs" id="nav-tab" role="tablist">
    {% for id, tab in form.tabs %}
        <a class="nav-item nav-link" id="nav-{{ id }}-tab" data-toggle="tab" href="#nav-{{ id }}" role="tab" aria-controls="nav-{{ id }}" aria-selected="true">{{ tab.label }}</a>
    {% endfor %}
</div>
<div class="tab-content" id="nav-tabContent">
    {% for id, tab in form.tabs %}
        <div class="tab-pane fade" id="nav-{{ index }}" role="tabpanel" aria-labelledby="nav-{{ id }}-tab">
{#            {% for field in row %}#}
{#                <div class="col-lg-{{ field.col }}">#}
{#                    {% include 'form/elements/'~ field.type ~ '.html.twig' with {element: field} %}#}
{#                </div>#}
{#            {% endfor %}#}
        </div>
    {% endfor %}
</div>
